import { DIRECTOR_PREVIZ_PALETTE } from "@director/project-schema";

/**
 * Appearance tokens for the locally shipped counterpart to Stage's core Human
 * component. These are visual defaults only: character color remains editable
 * from the inspector after creation.
 */
export const FLICK_HUMAN_DEFAULT_COLOR = DIRECTOR_PREVIZ_PALETTE.human;
export const FLICK_HUMAN_ARTICULATION_COLOR = DIRECTOR_PREVIZ_PALETTE.articulation;
