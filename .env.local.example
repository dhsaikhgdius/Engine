# Copy to .env.local and source in your shell:
#   cp .env.local.example .env.local && set -a && . ./.env.local && set +a

BLENDER_BIN=/usr/bin/blender
DIRECTOR_BLENDER_BIN=/usr/bin/blender
DIRECTOR_FFMPEG_PATH=/usr/bin/ffmpeg
DIRECTOR_FFPROBE_PATH=/usr/bin/ffprobe
STAGE_GATEWAY_URL=http://127.0.0.1:8787
DIRECTOR_DATA_DIRECTORY=data
