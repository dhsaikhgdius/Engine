---
title: 界面图标对照表
description: Director 各工作区图标按钮的功能说明、状态与待改进项。与代码同步维护，供逐项核对。
---

本文档与产品 UI 逐项对照维护。每个分区带有 **核对状态**，便于我们一起 review 并同步改代码。

最近核验：**2026-08-13**。

## 如何使用本文档

| 状态      | 含义                                |
| --------- | ----------------------------------- |
| ⬜ 待核对 | 已从代码整理，尚未与你确认行为/文案 |
| ✅ 已确认 | 功能与文档一致，无需改动            |
| 🔧 待改进 | 已确认存在问题，需改 UI 或文案      |

**建议顺序**：全局顶栏 → 3D 片场（视口 / 场景树 / 检查器 / 时间轴）→ 画布 → 视频 → Gallery → 设置类 Popover。

发现文档与界面不一致时，以当前 `frontend/director/src/comprehensive` 代码为准，并在「待改进」列记录。

---

## 1. 全局顶栏

**代码**：`frontend/director/src/comprehensive/App.tsx`  
**核对状态**：✅ 已确认

### 1.1 工作区切换

| 图标              | 可见标签   | 点击行为              | 激活态                     | 备注              |
| ----------------- | ---------- | --------------------- | -------------------------- | ----------------- |
| `LayoutDashboard` | 画布       | 切换到 Canvas 工作区  | 当前工作区 `aria-selected` | Tab 含图标 + 文字 |
| `Boxes`           | 3D 片场    | 切换到 Stage 工作区   | 同上                       |                   |
| `Film`            | 视频编辑器 | 切换到 Video 工作区   | 同上                       |                   |
| `Bot`             | Agent 工作区 | 切换到 Agent 工作区   | 同上                       | 独立会话界面      |

### 1.2 全局设置

| 图标                        | 标签 (aria-label)   | 点击行为                      | 激活态                         | 待改进            |
| --------------------------- | ------------------- | ----------------------------- | ------------------------------ | ----------------- |
| `Sun` / `Moon`              | 切换到浅色/深色模式 | 切换应用主题                  | 浅色模式下 `aria-pressed=true` |                   |
| `Languages` + `ChevronDown` | 界面语言            | 打开语言选择（zh-CN / en-US） | —                              | select 与图标组合 |

### 1.3 Stage 专用浮动按钮

冷启动时时间轴与右侧栏默认收起，左侧场景树保持展开。

| 图标              | 标签             | 显示条件                         | 点击行为        |
| ----------------- | ---------------- | -------------------------------- | --------------- |
| 底边拖条              | 展开下方栏       | Stage + 时间轴已启用且 dock 收起 | 上拉或点击展开时间轴 |
| 顶边拖条              | 调整时间轴高度   | Stage + 时间轴展开               | 点击收起；拖动调高度；下拉超过最小高度则收起 |
| 右边拖条              | 展开右侧栏       | Stage + 右侧栏收起               | 左拉或点击展开右侧栏 |
| 右栏左缘拖条          | 调整属性面板宽度 | Stage + 右侧栏展开               | 点击收起；拖动调宽度；向右拉过最小宽度则收起 |
| `Minimize2`           | 退出极简无框模式 | Stage 处于 frameless 全屏        | 退出无框模式         |

### 1.4 顶栏 Popover 触发器

详见各节；入口均在顶栏右侧：

| 图标        | 标签                | 面板                 | 可见工作区             |
| ----------- | ------------------- | -------------------- | ---------------------- |
| `RefreshCw` | 交换 / 专业格式交换 | 交换菜单             | Canvas / Stage / Video |
| `Settings2` | 视角手感            | 导航灵敏度、掌镜参数、舞台音效 | 主要 Stage             |
| `Gauge`     | 性能 {档位}         | 性能档位             | 全局                   |
| `Keyboard`  | 显示快捷键          | 快捷键列表           | 全局                   |

---

## 2. 3D 片场 — 右栏模式

**代码**：`frontend/director/src/comprehensive/app/layout/DirectorDeskShell.tsx`  
**核对状态**：✅ 已确认

| 图标                | 标签 | 点击行为                 | 激活态          | 待改进                      |
| ------------------- | ---- | ------------------------ | --------------- | --------------------------- |
| `SlidersHorizontal` | 属性 | 显示检查器（RightPanel） | `aria-selected` |                             |
| `Box`               | 建模 | 显示 Blender 实时   | 同上            |                             |
| `FolderOpen`        | 资源 | 显示 AssetLibraryPanel   | 同上            | Stage 下导入/模型库入口在此 |

---

## 3. 3D 视口工具栏

**代码**：`frontend/director/src/comprehensive/editor/canvas/ViewportToolbar.tsx`  
**核对状态**：✅ 已确认

容器：`aria-label="3D视口快捷工具"`。按钮为 **icon-only**，完整标签在 hover/focus 时通过 `.viewport-toolbar-label` 显示。

> **Stage 模式**：`导入全景图`、`导入本地模型`、`模型库` 不在视口栏，改在右侧 **资源** 面板（`assetActionsInSidebar=true`）。

### 3.1 选择与变换

| 图标            | 标签     | 点击行为                    | 激活 / 禁用                                |
| --------------- | -------- | --------------------------- | ------------------------------------------ |
| `LassoSelect`   | 套索选择 | 切换套索多选                | active=启用；disabled=角色漫游/掌镜/播放中 |
| `Move3D`        | 移动     | 变换模式：平移              | active=当前模式；disabled=漫游/掌镜        |
| `Rotate3D`      | 旋转     | 变换模式：旋转              | 同上                                       |
| `Scale3D`       | 缩放     | 变换模式：缩放              | 同上                                       |
| `Hand`          | 手型浏览 | 导航模式：手型（orbit/pan） | active=当前模式                            |
| `MousePointer2` | 游标浏览 | 导航模式：游标              | 同上                                       |

### 3.2 沉浸式模式

| 图标         | 标签                    | 点击行为          | 激活 / 禁用                              |
| ------------ | ----------------------- | ----------------- | ---------------------------------------- |
| `Footprints` | 角色漫游 / 退出角色漫游 | 切换 Player 模式  | active=漫游中；disabled=掌镜或无可用角色 |
| `Crosshair`  | 开始掌镜 / 退出掌镜     | 切换 Camera Pilot | active=掌镜中；disabled=漫游中           |

### 3.3 资产与机位（非 Stage sidebar 模式）

| 图标        | 标签         | 点击行为                  |
| ----------- | ------------ | ------------------------- |
| `ImagePlus` | 导入全景图   | 打开 .jpg/.png/.webp 选择 |
| `Box`       | 导入本地模型 | 打开 .fbx/.obj/.glb 选择  |
| `Boxes`     | 模型库       | 打开浮动模型库面板        |
| `Video`     | 添加机位     | 从当前视口快照添加相机    |

### 3.4 视口显示与布局

| 图标         | 标签                | 点击行为                                     | 激活 / 禁用                       | 待改进                     |
| ------------ | ------------------- | -------------------------------------------- | --------------------------------- | -------------------------- |
| `Ratio`      | 选择画幅比例        | 打开画幅比例面板                             | —                                 |                            |
| `Tag`        | 视口标签            | 切换 `showLabels`（角色/机位/标注/测量文字） | active=标签可见                   | 与场景面板「视口标签」同步 |
| `LayoutGrid` | 四视图 / 退出四视图 | 切换 quad 布局                               | active=四视图；disabled=漫游/掌镜 | 与窗格内「最大化」易混淆   |
| `Expand`     | 全屏                | 切换 frameless 无框模式                      | —                                 |                            |

### 3.5 截图

| 图标      | 标签         | 点击行为                     |
| --------- | ------------ | ---------------------------- |
| `Camera`  | 当前视角截图 | 单帧 capture 到当前/新建机位 |
| `Grid2X2` | 四方位截图   | 四向 capture                 |
| `Grid3X3` | 十二方位截图 | 十二向 capture               |

**四视图模式下**工具栏仅保留：退出四视图、视口标签、四方位截图、全屏。

---

## 4. 视口内浮层与 HUD

**核对状态**：✅ 已确认

### 4.1 机位 PiP 预览

| 图标              | 标签              | 行为                          |
| ----------------- | ----------------- | ----------------------------- |
| `Lock` / `Unlock` | 锁定/解除相机视图 | 锁定 PiP 取景，`aria-pressed` |

### 4.2 四视图窗格（QuadViewportRenderer）

| 图标                      | 标签                    | 行为           |
| ------------------------- | ----------------------- | -------------- |
| `Focus`                   | 适配视图                | 重置该窗格视图 |
| `Maximize2` / `Minimize2` | 最大化视图 / 恢复四视图 | 单窗格最大化   |

### 4.3 角色漫游 HUD（PlayerModeHud）

| 图标     | 可见文本      | 行为         | 待改进                     |
| -------- | ------------- | ------------ | -------------------------- |
| `Eye`    | 第一/第三人称 | 切换视角     | 可见文案即 accessible name |
| `Plane`  | 步行/飞行     | 切换移动模式 | 同上                       |
| `Circle` | 记录移动      | 切换录制     | 同上                       |
| `LogOut` | —             | 退出漫游     | 有 aria-label              |

技能施放栏使用 `Sparkles`，槽位为内联 SVG 符文；开关文案为「开 / 关」。

### 4.4 掌镜 HUD（CameraPilotHud）

| 图标             | 标签           | 行为          |
| ---------------- | -------------- | ------------- |
| `CornerDownLeft` | 记录当前轨迹点 | 记录 waypoint |
| `LogOut`         | 退出掌镜模式   | 退出          |

---

## 5. 场景树（ObjectTreePanel）

**核对状态**：✅ 已确认

### 5.1 类型装饰（不可点击）

| 图标     | 含义             |
| -------- | ---------------- |
| `User`   | 角色             |
| `Users`  | 群众/群组        |
| `Box`    | 几何体/模型/组合 |
| `Camera` | 摄像机           |

### 5.2 行内操作

| 图标                           | 典型 aria-label   | 行为                            |
| ------------------------------ | ----------------- | ------------------------------- |
| `ChevronRight` / `ChevronDown` | 展开/收起 {name}  | 展开子项或分组                  |
| `Pencil`                       | 重命名 {name}     | 内联重命名                      |
| `Link2`                        | 展开/收起参考绑定 | 显示参考绑定面板                |
| `MoreHorizontal`               | 打开对象操作      | 浮动菜单（定位/显隐/锁定/删除） |
| `Crosshair`                    | 在视图中定位      | 聚焦视口到对象                  |
| `Eye` / `EyeOff`               | 隐藏/显示对象     | 切换 `visible`                  |
| `Lock` / `Unlock`              | 锁定/解除锁定     | 切换 `locked`                   |
| `Trash2`                       | 删除              | 删除对象或群众                  |

### 5.3 高级编辑（`<details>` 摘要：`Scan` 高级编辑）

| 图标                              | 文本         | 行为                      |
| --------------------------------- | ------------ | ------------------------- |
| `RotateCcw`                       | 重置变换     | 重置选中对象变换          |
| `EyeOff`                          | 隔离         | 仅显示选中                |
| `Eye`                             | 全部显示     | 恢复全部可见              |
| `Group`                           | 组合         | 创建组合（≥2 选中）       |
| `Crosshair`                       | 应用枢轴     | 应用 pivot                |
| `AlignHorizontalDistributeCenter` | 均匀分布     | 分布对象（≥3 选中）       |
| `MessageSquare`                   | 添加到对象   | 添加场景标注              |
| `Ruler`                           | 测量两个对象 | 添加测量（需 2 对象选中） |

---

## 6. 检查器面板

**核对状态**：✅ 已确认

### 6.1 场景（ScenePanel）

| 图标       | 标签                     | 行为                     |
| ---------- | ------------------------ | ------------------------ |
| `Plus`     | 添加灯光 / 添加剖切平面  | 新增                     |
| `Trash2`   | 删除灯光 / 剖切面 / 全景 | 删除                     |
| `ImageOff` | —                        | 未连接全景时的空状态装饰 |

「显示与吸附」区为 checkbox，无图标：**视口标签**、网格吸附、地面。

### 6.2 相机（CameraPanel）

| 图标                 | 标签             | 行为             |
| -------------------- | ---------------- | ---------------- |
| `Move3D`             | 在画布中移动机位 | 进入视口移动机位 |
| `Camera`             | 截图到画布       | 当前机位 capture |
| `PackageOpen`        | 导出 AI 控制包   | 导出控制包       |
| `Send`               | 发送到画布       | 截图发到 Canvas  |
| `Eye`                | 查看截图         | 打开查看器       |
| `Download`           | 下载             | 下载渲染/截图    |
| `Upload`             | 发送到 ComfyUI   | 上传视频         |
| `ZoomIn` / `ZoomOut` | 放大/缩小图片    | 查看器缩放       |
| `X`                  | 关闭查看器       | 关闭             |
| `Trash2`             | 删除截图 / 清空  | 删除             |

### 6.3 角色 / 道具

| 图标              | 标签         | 行为           | 待改进                                      |
| ----------------- | ------------ | -------------- | ------------------------------------------- |
| `ArrowDownToLine` | 贴地放置     | 让道具落到地面 | Prop 面板已中文化                           |
| `ArrowDownToLine` | Down 2 Earth | 让角色落到地面 | 🔧 角色面板按钮文案仍为英文，title 才是中文 |

### 6.4 资源库（AssetLibraryPanel，右栏「资源」）

| 图标         | 文本                | 行为           |
| ------------ | ------------------- | -------------- |
| `Upload`     | 导入模型            | 文件选择       |
| `Sparkles`   | AI 生成 3D          | 打开生成对话框 |
| `ScanSearch` | 参考图重建          | 打开重建对话框 |
| `Boxes`      | 程序化建模          | 打开程序化工具 |
| `ImagePlus`  | 导入贴图 / 导入全景 | 文件选择       |

---

## 7. 时间轴（DirectorTimelineDock）

**核对状态**：✅ 已确认

工具栏图标统一 `size: 14`、`strokeWidth: 1.75`（`TOOLBAR_ICON`）。轨头装饰图标仍为 13px，不在此表。

### 7.1 视图 Tab

| 图标           | 文本       | 行为       |
| -------------- | ---------- | ---------- |
| `Rows3`        | 轨道 / 帧  | 时间轴视图 |
| `Images`       | 场景缩略图 | 场景浏览器 |
| `Clapperboard` | 分镜       | 分镜面板   |

### 7.2 播放与循环

| 图标             | aria-label     | 行为                |
| ---------------- | -------------- | ------------------- |
| `Play` / `Pause` | 播放/暂停动画  | 传输条：预览播放    |
| `SkipBack`       | 回到时间轴开头 | reset 到 frameStart |
| `Repeat`         | 循环播放       | 切换 loop           |

### 7.3 创建与编辑

| 图标                | aria-label       | 行为                 |
| ------------------- | ---------------- | -------------------- |
| `Film`              | 添加分镜到播放头 | 添加 storyboard shot |
| `ListPlus`          | 添加轨道         | 为选中对象添加空轨道 |
| `Route`             | 添加动作         | 打开轨迹预设         |
| `WandSparkles`      | 动画配方         | 打开配方面板         |
| `UserRound`         | 姿势关键帧       | 应用角色动作         |
| `Plus`              | 添加区块         | 添加动作区块         |
| `Replace`           | 替换所选动作区块 | 替换当前区块         |
| `SlidersHorizontal` | 编辑动作区块参数 | 打开参数面板         |

### 7.4 排练、录制与导出

| 图标                    | 标签                       | 行为                             |
| ----------------------- | -------------------------- | -------------------------------- |
| `MonitorPlay` / `Pause` | 排练 / 暂停排练            | 视口预览，与传输条共用播放开关   |
| `Circle`（实心）        | 记录 / 开始记录 / 继续记录 | 录制点；CSS `fill: currentColor` |
| `Pause`                 | 暂停记录                   | 暂停渲染                         |
| `Square`                | 停止记录                   | 停止渲染                         |
| `Video`                 | 自动导出 IN/OUT 视频       | 导出视频                         |
| `Layers`                | 导出确定性 IN/OUT 帧包     | 逐帧 PNG 包                      |
| `Database`              | 导出多模态 IN/OUT 数据包   | 通道 + 参数数据包                |
| `CircleX`               | 取消导出                   | 取消进行中的导出                 |

---

## 8. 画布工作区（CanvasWorkspace）

**核对状态**：✅ 已确认

工具栏 `aria-label="画布操作"`。主要图标：

| 图标              | 标签               | 行为          |
| ----------------- | ------------------ | ------------- |
| `Plus`            | 添加节点           | 打开添加菜单  |
| `MousePointer2`   | 选择               | 工具：select  |
| `Hand`            | 手型浏览           | 工具：hand    |
| `Cable`           | 连接节点           | 工具：connect |
| `Undo2` / `Redo2` | 撤销 / 重做        | 历史          |
| `StickyNote`      | 添加便签           | addNote       |
| `LayoutGrid`      | 添加分区           | addSection    |
| `FileText`        | 导入剧本           | Fountain 模态 |
| `PanelsTopLeft`   | 添加分组           | addFrame      |
| `Workflow`        | 自动排列依赖图     | autoLayoutDag |
| `Play`            | 运行 Canvas 依赖图 | 运行流水线    |
| `Square`          | 取消 Canvas 流水线 | 取消运行      |

添加菜单内：`Upload` 上传、`Type` 文本备注、`Images`/`WandSparkles`/`Mic2`/`Music2` 生成节点、`Lightbulb` 灵感。

---

## 9. 视频编辑器（VideoEditorWorkspace）

**核对状态**：✅ 已确认

| 区域   | 图标                  | 标签          | 行为           |
| ------ | --------------------- | ------------- | -------------- |
| 标题栏 | `Download`            | 导出视频      | 打开导出对话框 |
| 传输条 | `SkipBack`            | 回到开头      | playhead=0     |
| 传输条 | `Play` / `Pause`      | 播放/暂停     | 切换           |
| 传输条 | `Magnet`              | 磁吸          | 切换 snap      |
| 传输条 | `Scissors`            | 分割          | 分割剪辑       |
| 传输条 | `Type`                | 添加文字      | 添加文字轨     |
| 轨头   | `Eye` / `EyeOff`      | 隐藏/显示画面 | 切换 visible   |
| 轨头   | `Volume2` / `VolumeX` | 静音          | 切换 muted     |
| 轨头   | `Lock` / `Unlock`     | 锁定          | 切换 locked    |
| 轨头   | `ZoomIn` / `ZoomOut`  | 缩放时间线    | 缩放           |

---

## 10. Gallery 工作区

**核对状态**：✅ 已确认

| 图标             | aria-label | 行为          | 激活态         |
| ---------------- | ---------- | ------------- | -------------- |
| `Grid2X2`        | 网格       | grid 视图     | `aria-pressed` |
| `Columns3`       | 瀑布流     | masonry 视图  | 同上           |
| `List`           | 列表       | list 视图     | 同上           |
| `Film`           | 时间线     | timeline 视图 | 同上           |
| `Upload`         | 导入       | 导入文件      |                |
| `Sparkles`       | 生成       | 打开生成面板  |                |
| `ArchiveRestore` | 导出工程   | 导出          |                |

卡片操作：`CheckSquare` 多选、`Star` 收藏、`Tags` 标签、`Send` 发送到工作区。

---

## 待改进汇总（Backlog）

与 2026-08-13 代码核对后仍未关闭的项。已关闭：道具检查器「贴地放置」、PlayerModeHud 可见文案、§3 已注明 Stage 资产入口、时间轴工具栏图标撞车（播放/排练/`Clapperboard`/`FileArchive`/`Repeat2`）。

| 优先级 | 区域       | 问题                             | 建议                             |
| ------ | ---------- | -------------------------------- | -------------------------------- |
| P1     | 视口工具栏 | 默认 icon-only，新用户难发现功能 | 首访提示或持久 tooltip 选项      |
| P1     | 角色检查器 | `Down 2 Earth` 英文按钮          | 改为「贴地放置」并 i18n          |
| P2     | 模型库按钮 | 面板打开时 toolbar 无 active 态  | 面板打开时高亮 `Boxes`           |

---

## 相关文档

- [3D 编辑器概览](/zh/editor/)
- [场景与资产](/zh/editor/scenes-and-assets/) — 模型库、导入、角色漫游
- [相机](/zh/editor/cameras/) — 掌镜、PiP、四视图
- [动画与时间线](/zh/editor/animation/)
- [画布与视频编辑器](/zh/editor/canvas-video/)
- [项目 Gallery](/zh/editor/gallery/)
