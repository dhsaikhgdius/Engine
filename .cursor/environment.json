{
  "install": "git submodule update --init --depth 1 vendor/deepseek-harness\nnpm ci\nnpx playwright install chromium\nbash tools/scripts/install-dcc-runtimes.sh || true",
  "start": "echo \"Director cloud env ready: Node $(node -v), npm $(npm -v), Blender $(command -v blender || echo missing), Unity $(command -v unity || echo missing)\"",
  "terminals": [
    {
      "name": "director-dev",
      "command": "npm run dev",
      "description": "Director Gateway (:8787) and Vite UI (:5175). Both bind 127.0.0.1; Cursor forwards them to the local machine."
    }
  ],
  "ports": [
    { "name": "Director UI", "port": 5175 },
    { "name": "Director Gateway", "port": 8787 }
  ]
}
