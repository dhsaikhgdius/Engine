// Keep the documentation site isolated from the application-level Tailwind
// PostCSS configuration in the repository root. Starlight Nova ships compiled
// CSS and does not require the app's Tailwind v3 transform.
export default {
  plugins: [],
};
